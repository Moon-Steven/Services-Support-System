# Persona 模块 — 产品需求文档 (PRD)

> 版本：v1.3 · 最后更新：2026-04-08
> 归属：Around the Clock 产品线
> 变更记录：v1.3 完成审核工作台业务闭环（通过/驳回/换名言/润色/调雷达全部落库），取消“待人工选择”独立分流页签，改为统一待审队列 + 低匹配标记；统计口径统一为“当前角色待审队列”

---

## 1. Overview

### 问题陈述

客户在使用 AI Agent 管理广告投放时，存在两层障碍：

1. **理解障碍**：AI Agent 的 6 个能力维度（留存、探索、精准、效率、风控、创意）缺乏可视化呈现，客户无法判断"AI 在哪些方面帮了我"
2. **情感障碍**：即使理解了能力数据，面对纯数字的雷达图，客户仍然感到"在和机器打交道"，缺乏拟人化的信任锚点

这两层障碍导致客户对高价值自动化操作（如凌晨自动调价、素材淘汰）的接受度偏低。

### 解决方案与假设

Persona 模块由两个**独立但互补**的组件构成，各自解决一层障碍：


| 组件       | 解决的障碍 | 作用                           | 优先级      |
| -------- | ----- | ---------------------------- | -------- |
| **能力雷达** | 理解障碍  | 可视化 6 维能力 + 历史趋势，回答"AI 擅长什么" | **核心**   |
| **名人名言** | 情感障碍  | 赋予 Agent 可感知的价值观人格，降低"机器感"   | **辅助增强** |


> **关键假设 [待验证]**：名言作为情感层包装，能够提升客户对雷达数据的关注度和正面解读意愿。此假设需在 V1 内部试用阶段收集定性反馈验证。如果验证失败，名言可独立移除而不影响雷达的核心价值。

### 目标

**V1 目标（内部验证）**：完成数据模型 + 前端组件 + 种子数据，在内部系统中跑通展示流程，收集交付/销售团队的定性反馈。

**V2 目标（灰度上线）**：接入真实数据源，对 20% 客户灰度展示，验证信任度假设。

### 目标用户


| 角色                 | 使用场景                                 | 阶段         |
| ------------------ | ------------------------------------ | ---------- |
| **交付团队**           | 在内部客户详情页查看 Persona，评估是否准确反映 Agent 行为 | V1         |
| **销售**             | 使用 Persona 作为客情沟通工具，向客户解读 AI 能力      | V1         |
| **行业运营**           | 审核名言的行业适配性和品牌安全性                     | V2         |
| **名言内容策划人**（品牌/运营） | 发现、录入、标注名言，维护名言库的供给侧                 | V2         |
| **算法/数据团队**        | 定义雷达维度的计算公式，标注名言亲和力权重                | V2         |
| **广告主 / 客户**       | 在客户端查看 Persona，理解 AI Agent 的特征       | **V2 灰度后** |


### 范围

**V1.3（内部验证可演示版）包含：**

- 6 维能力雷达可视化（含历史对比）
- 名言库种子数据（10 条，预审核状态）
- 名言与雷达的匹配算法
- 客户详情页（内部系统）侧栏集成
- 三级审核工作台（交付/行运/销售）与审核详情 Drawer
- 审核动作闭环：通过、驳回、手动换名言、文案润色、手动调雷达（含审计日志）
- 总览页与客户详情页实时联动刷新（事件驱动）

**V1.3 明确不包含：**

- 对客展示（V2 灰度后开放）
- 雷达分数的真实计算引擎（当前使用 mock 并标注"演示数据"）
- 对接外部通知通道（飞书预留），当前仅站内可见
- 持久化数据库与多人并发冲突处理（当前为内存 mock）

> **⚠️ V1 数据诚实度约束**：V1 的雷达分数为策略团队基于经验设定的演示数据，非 Agent 行为真实计算结果。所有 V1 界面必须标注"演示数据"标记，禁止以任何形式暗示这是实时计算结果。

**页面级落地要求（强制）：**

- 客户详情页 Persona 卡片：显示 `演示数据` 标签
- Persona 审核详情 Drawer：显示 `演示数据` 标签
- Persona 总览页：页面级提示“当前为演示数据”
- 任何导出/截图用于外部沟通时：必须附带“演示数据”说明

---

## 2. Background & Context

### 为什么是现在

1. **Around the Clock 已上线** — 正面策略事件的审核发布流程已具备，Persona 作为"AI 做了什么"到"AI 是谁"的自然延伸，基础设施就绪
2. **内部反馈 [定性]** — 销售团队在客情沟通中多次反馈"缺乏直观工具向客户解释 AI 在做什么"，目前依赖口头描述，效率低且不一致
3. **行业趋势 [观察]** — 广告技术行业开始出现 Agent 能力透视类功能（如 Google Ads 的"自动出价策略强度"指示器），方向值得探索

> **注**：以上第 2、3 点为团队内部观察，非量化数据。V1 的一个目标是通过内部试用将这些定性判断转化为可量化的证据。

### 与现有系统的关系

```
┌───────────────────────────────────────────────────┐
│                 客户详情页（内部系统）               │
│  ┌──────────┐  ┌────────────────────────────────┐ │
│  │ Persona  │  │   Data Results & AI Decisions  │ │
│  │ · 雷达   │  │   · KPI 指标卡片                │ │
│  │ · 名言   │  │   · 趋势图表                    │ │
│  │          │  │   · 策略决策卡片                │ │
│  └──────────┘  ├────────────────────────────────┤ │
│                │   Around the Clock             │ │
│                │   · 24h Timeline               │ │
│                │   · 审核工作台                   │ │
│                └────────────────────────────────┘ │
└───────────────────────────────────────────────────┘
```

**产品逻辑链：**

```
Persona（AI 是谁）→ Around the Clock（AI 做了什么）→ Data Results（效果如何）
```

V2 实现后，正面策略事件经审核发布将反馈至 Persona 雷达的维度分数，形成正向循环。V1 阶段此循环暂未打通。

### 关键指标基线


| 指标              | 当前值 | 目标                  | 说明           |
| --------------- | --- | ------------------- | ------------ |
| 客户对 AI 决策的信任度   | 无基线 | V1 内部定性评估；V2 建立量化基线 | 需定义测量方法      |
| 销售解释 AI 能力的沟通效率 | 无基线 | V1 收集交付/销售主观反馈      | 定性 → 定量      |
| Persona 卡片关注度   | N/A | V2 灰度阶段建立           | 需定义"关注"的埋点口径 |


---

## 3. User Stories & Requirements

### P0 — Must Have（V1 交付）

#### US-01：内部用户查看 AI Agent 能力画像

> **As a** 交付团队成员/销售，**I want to** 在客户详情页看到该客户 AI Agent 的能力雷达和特征名言，**so that** 我能向客户准确解释 AI 在管理其投放时的能力分布。

**验收标准：**

- 6 维雷达图展示当前分数（Retention / Exploration / Precision / Efficiency / Risk Ctrl / Creativity）
- 叠加展示 7 天前分数作为对比层（灰色虚线）
- 显示评估周期日期范围
- 显示匹配的名人名言（含作者、年份）
- 雷达下方分数表显示每维度的变化 delta
- **界面显著标注"演示数据"**（V1 约束）

#### US-02：名言库种子数据预审

> **As a** 产品负责人，**I want to** 名言库的种子数据在入库时已经过品牌安全预审，**so that** V1 内部试用期间不会出现不当内容。

**V1 验收标准（简化版）：**

- 种子名言在代码入库前由品牌/运营负责人口头确认适用性
- 名言数据结构包含 `status` 字段，V1 种子全部标记为 `available`
- 数据结构预留 `reviewedBy` / `reviewedAt`，V1 填写预审信息
- 数据结构预留 `retiredReason`，V1 暂不使用

> 注：完整的名言入库审核 UI（draft → under_review → available → retired 全流程）在 V2 交付。V1 通过代码审查 + 口头确认替代。

#### US-03：名言可替换数据模型预留

> **As a** 系统架构师，**I want to** V1 的数据模型包含审核与替换状态字段，**so that** V2 构建审核 UI 时无需重构数据层。

**V1.3 验收标准（数据层 + 页面联动）：**

- `PersonaSnapshot` 包含 `lockStatus` 字段（`auto_selected / in_review / unlocked / pending_manual`）
- `canAlgorithmReplaceQuote()` 函数实现“仅 pending_manual 阻断自动替换”的逻辑
- 种子数据以 `auto_selected / in_review / pending_manual` 为主，覆盖主流转与低匹配分支
- 审核页面可触发状态流转并写入审计日志（通过/驳回/换名言/润色/调雷达）

### P0 — Must Have（V2 交付）

#### US-04：名言库完整生命周期管理

> **As a** 名言内容策划人，**I want to** 通过审核流程录入和管理名言，**so that** 只有通过合规审核的名言才能进入可选池。

**验收标准：**

- 名言状态生命周期 UI：`draft → under_review → available → retired`
- 只有 `status === 'available'` 的名言可被匹配算法选中
- 下架分级：`retired_normal`（自然过期）和 `retired_safety`（安全原因）
- `retired_safety` 触发强制重匹配（见 US-06）

#### US-05：审核通过后的名言可随新周期自动替换

> **As a** 销售，**I want to** 审核通过后的 Persona 名言可随新周期自动更新，**so that** 对客展示持续反映 Agent 最新能力变化，减少人工维护成本。

**验收标准：**

- 匹配算法在常规状态下可自动替换名言（`pending_manual` 除外）
- 新周期重新匹配后状态回到 `auto_selected`，需再次走完三级审核
- 无“锁定/解锁”操作入口
- 对客一致性通过“周期版本 + 审核流转”保障，而非人工锁定

#### US-06：安全下架触发强制重匹配

> **As a** 品牌合规审核员，**I want to** 因安全原因下架名言时自动触发所有引用客户重匹配，**so that** 有风险内容不会继续在客户界面展示。

**验收标准：**

- `retired_safety` 操作触发：遍历所有引用该名言的 `PersonaSnapshot`
- 相关 snapshot 标记为“待重新匹配/待审核”并进入下一轮审核流
- 写入系统审计日志，记录原因和影响范围
- 触发匹配算法为受影响客户重新推荐名言

### P1 — Should Have

#### US-07：名言与雷达的智能匹配

> **As a** 交付团队成员，**I want to** 系统基于客户 AI Agent 的能力雷达自动推荐最匹配的名言，**so that** 名言与 Agent 实际表现保持一致，减少人工选择成本。

**验收标准：**

- 基于余弦相似度计算名言亲和力向量与客户雷达向量的匹配度
- 行业标签匹配加分（同行业 +0.05 bonus）
- 输出匹配分数 `quoteMatchScore`（0–1）
- 仅从 `available` 状态的名言池中选择
- 匹配分数 < 0.5 时标记低匹配风险，并优先出现在当前角色的待审队列中

#### US-08：Persona 状态标签展示

> **As a** 交付团队成员/销售，**I want to** 在客户详情页看到 Persona 的审核状态标签，**so that** 我知道当前名言是否已审核、是否处于待处理状态。

**验收标准：**

- `in_review` → 橙色标签"审核中"
- `auto_selected` → 灰色标签"算法推荐"
- `unlocked` → 灰色标签"待重新匹配"
- `pending_manual` → 红色标签"待处理（低匹配）"（内部兼容状态，不单独分流展示）

#### US-09：客户绑定业务审核

> **As a** 交付团队成员，**I want to** 对算法推荐的名言-客户绑定进行业务审核，**so that** 确认名言适合该客户的具体情境后再展示。

**验收标准：**

- 复用 Around the Clock 的三级审核架构（交付 → 运营 → 销售）
- 交付审核：核查名言与客户雷达的匹配合理性
- 运营审核：核查名言是否符合该客户所在行业的调性
- 销售审核：最终确认是否适合对客展示、是否影响客情
- SLA 时效管理（复用现有 SLA 交通灯机制）

> **名言库入库审核 vs 客户绑定审核的关系**：这是两个独立流程。入库审核解决"这条名言本身是否安全可用"（合规层，一次性）；绑定审核解决"这条名言是否适合这个客户"（业务层，每次绑定）。一条名言通过入库审核 ≠ 适合所有客户。例如：Buffett 的风险名言通过了合规审核，但绑定到一个追求激进增长的客户上可能不合适。

### P2 — Nice to Have

#### US-10：维度分数剧变告警

> **As a** 交付团队成员，**I want to** 在单维度分数周环比变化超过 15% 时收到提醒，**so that** 我可以核查原因并附加变更说明。

**验收标准：**

- 系统自动检测维度变化幅度
- 超阈值时在 Persona 卡片标注告警图标
- 支持附加变更说明

---

## 4. Design & UX

### 核心流程：数据生成 → 三级审核 → 对客展示

这是 Persona 模块的**主干流程**。任何 Persona 内容必须走完此流程才可对客展示，无例外。

```
┌─────────────────────────────────────────────────────────────────────┐
│  阶段 0：数据生成（HUI 系统）                                         │
│                                                                     │
│  HUI 系统按日滚动（约 1 天完成一次）自动执行：                            │
│  ① 计算客户 AI Agent 的 6 维雷达分数（基于 Agent 行为数据）              │
│  ② 从名言可选池（status=available）中匹配最佳名言                       │
│  ③ 生成 PersonaSnapshot（lockStatus=auto_selected）                  │
│                                                                     │
│  输出：雷达分数 + 匹配名言 + 匹配分数                                   │
│  状态：auto_selected（算法推荐，未经人工审核，不可对客展示）                │
└────────────────────────┬────────────────────────────────────────────┘
                         │
                         │ 进入统一审核队列
                         │ （< 0.5 标记"低匹配风险"，但仍进入审核流水线）
                         │
┌────────────────────────▼────────────────────────────────────────────┐
│  阶段 1：一审 — 交付团队                                              │
│                                                                     │
│  审核内容：                                                          │
│  · 雷达分数是否合理反映 Agent 近期行为？（对照 Around the Clock 事件）    │
│  · 名言文本是否通顺、无歧义？                                          │
│  · 分数变化幅度是否需要附加说明？（单维度变化 > 15% 需标注）               │
│                                                                     │
│  操作：通过 → 进入二审 ｜ 驳回 → 退回 HUI 重新生成 / 人工调整            │
│  状态变更：auto_selected → in_review                                  │
└────────────────────────┬────────────────────────────────────────────┘
                         │ 通过
┌────────────────────────▼────────────────────────────────────────────┐
│  阶段 2：二审 — 行业运营（行运）                                       │
│                                                                     │
│  审核内容：                                                          │
│  · 名言归属人是否存在品牌安全风险？                                      │
│  · 名言调性是否符合该客户所在行业？                                      │
│  · 名言与雷达的匹配是否合理？（如风控型客户配了创意型名言 → 不合适）         │
│                                                                     │
│  操作：通过 → 进入三审 ｜ 驳回 → 退回交付 ｜ 润色文案 → 继续流转          │
└────────────────────────┬────────────────────────────────────────────┘
                         │ 通过
┌────────────────────────▼────────────────────────────────────────────┐
│  阶段 3：三审 — 销售                                                  │
│                                                                     │
│  审核内容：                                                          │
│  · 结合客情判断：此内容是否适合当前展示给该客户？                          │
│  · 是否与近期客户沟通中的承诺/说法一致？                                  │
│  · 是否存在过度承诺风险？（如分数全部 > 90）                              │
│                                                                     │
│  操作：通过 → 发布展示 ｜ 驳回 → 退回行运 ｜ 微调 → 发布展示             │
└────────────────────────┬────────────────────────────────────────────┘
                         │ 通过
┌────────────────────────▼────────────────────────────────────────────┐
│  阶段 4：发布 & 对客展示                                              │
│                                                                     │
│  状态变更：in_review(at_sales) → auto_selected（已完成审核版本）          │
│  记录：review_pass（销售）、snapshot version                           │
│                                                                     │
│  此刻起：                                                            │
│  · Persona 卡片在客户界面生效展示                                      │
│  · HUI 下一周期生成新数据时，算法可自动重匹配并生成新版本                   │
│  · 新版本仍需重新走阶段 1–3，确保对客内容受审核控制                         │
└─────────────────────────────────────────────────────────────────────┘
```

#### 状态机精确定义

以下为 `PersonaSnapshot.lockStatus` 的合法状态转换（去锁定版）：

- `auto_selected` → 提交一审 → `in_review(at_delivery)`
- `in_review(at_delivery)` → 通过 → `in_review(at_ops)`；驳回 → `auto_selected`
- `in_review(at_ops)` → 通过 → `in_review(at_sales)`；驳回 → `in_review(at_delivery)`
- `in_review(at_sales)` → 通过/微调通过 → `auto_selected`（进入展示版本）；驳回 → `in_review(at_ops)`
- 任意状态匹配分 < 0.5 时进入 `pending_manual`，手动指定后回到 `auto_selected`
- 安全下架触发受影响客户回到“待重新匹配/待审核”链路，不存在“解锁”动作

> `pending_manual` 为兼容态：用于标识“低匹配需人工处理”的内部状态。产品界面不再单独分流为“待人工选择”页签，而是在统一待审队列中通过低匹配标红、操作按钮（进入处理）引导人工修正。

**驳回路径的精确语义：**


| 驳回场景    | 发起人 | Snapshot 状态变更                                  | 显示标记                   | 后续处理                              |
| ------- | --- | ---------------------------------------------- | ---------------------- | --------------------------------- |
| 交付驳回    | 交付  | `in_review(at_delivery)` → `auto_selected`     | 标记"退回任务" + 附带驳回原因      | 可人工调整分数/换名言后重新提交，或等待 HUI 下一周期重新生成 |
| 行运驳回→交付 | 行运  | `in_review(at_ops)` → `in_review(at_delivery)` | 标记"退回任务：行运退回" + 附带驳回原因 | 交付可修改后重新提交行运                      |
| 销售驳回→行运 | 销售  | `in_review(at_sales)` → `in_review(at_ops)`    | 标记"退回任务：销售退回" + 附带驳回原因 | 行运可修改后重新提交销售                      |


> **原则**：驳回只退一级，不跨级退回。每次驳回必须附带原因，原因对下游可见。

#### 审核时效（SLA）

复用 Around the Clock 的 SLA 交通灯机制：


| 审核阶段   | SLA 时限 | 超时处理                           |
| ------ | ------ | ------------------------------ |
| 一审（交付） | 24h    | 黄灯告警 → 48h **升级通知**至交付主管，不自动流转 |
| 二审（行运） | 24h    | 黄灯告警 → 48h **升级通知**至运营主管，不自动流转 |
| 三审（销售） | 48h    | 黄灯告警 → 72h 自动拦截（不展示），标记待处理     |


> **不自动流转的理由**：一审核查"分数是否真实反映 Agent 行为"，二审核查"品牌安全"。跳过任何一级等于在未校验的基础上做后续审核，风险不可接受。超时应升级告警而非默认"不审也行"。

#### 自动重匹配 → 重新审核

```
当前展示版本（auto_selected，已完成三级审核）
   │
   │ HUI 新周期生成 / 安全下架触发重匹配
   │
   ▼
auto_selected（新版本）→ 重走阶段 1–4
```

#### 权限矩阵


| 操作            | 交付     | 行运     | 销售     | 内容策划人 | 合规审核员 | 系统  |
| ------------- | ------ | ------ | ------ | ----- | ----- | --- |
| **审核工作台**     |        |        |        |       |       |     |
| 查看全部待审事件      | ✅      | ✅      | ✅      | ○     | ○     | —   |
| 一审通过/驳回       | ✅      | —      | —      | —     | —     | —   |
| 二审通过/驳回/润色    | —      | ✅      | —      | —     | —     | —   |
| 三审通过/驳回/微调    | —      | —      | ✅      | —     | —     | —   |
| 手动换名言         | ✅      | ✅      | ✅      | —     | —     | —   |
| 批量操作          | ✅（仅本级） | ✅（仅本级） | ✅（仅本级） | —     | —     | —   |
| **名言库管理**     |        |        |        |       |       |     |
| 查看名言库         | ✅      | ✅      | ✅      | ✅     | ✅     | —   |
| 录入名言（草稿）      | —      | —      | —      | ✅     | —     | —   |
| 合规审核通过/驳回     | —      | —      | —      | —     | ✅     | —   |
| 正常下架          | —      | ✅      | —      | ✅     | ✅     | —   |
| 安全下架          | —      | —      | —      | —     | ✅     | —   |
| **客户绑定**      |        |        |        |       |       |     |
| 查看 Persona 总览 | ✅      | ✅      | ✅      | ○     | ○     | —   |
| 触发重匹配（安全下架触发） | —      | —      | —      | —     | —     | ✅   |


> ✅ = 有权限，○ = 只读查看，— = 不可见/不可操作

#### 消息通知


| 触发事件             | 接收人         | 通知渠道      | 通知内容                                                |
| ---------------- | ----------- | --------- | --------------------------------------------------- |
| HUI 生成新 Snapshot | 交付（该客户负责人）  | 站内待办（当前） + 飞书（预留） | "WaveBone 的 Persona 数据已更新，请审核"                      |
| 交付通过 → 进入二审      | 行运（该行业负责人）  | 站内待办（当前） + 飞书（预留） | "WaveBone Persona 已通过交付审核，请行运审核"                    |
| 行运通过 → 进入三审      | 销售（该客户负责人）  | 站内待办（当前） + 飞书（预留） | "WaveBone Persona 已通过行运审核，请确认发布"                    |
| 任意级别驳回           | 被退回方        | 站内待办（当前） + 飞书（预留） | "WaveBone Persona 被[行运]退回，原因：…"                     |
| SLA 黄灯（临近超时）     | 当前审核人       | 站内提醒      | "WaveBone Persona 审核即将超时（剩余 4h）"                    |
| SLA 红灯（超时升级）     | 当前审核人 + 其主管 | 站内待办（当前） + 飞书（预留） | "WaveBone Persona 审核已超时，已升级通知"                      |
| 三审超时自动拦截         | 销售 + 销售主管   | 站内待办（当前） + 飞书（预留） | "WaveBone Persona 因超时已被自动拦截"                        |
| 安全下架触发重匹配        | 所有受影响客户的销售  | 站内待办（当前） + 飞书（预留） | "名言[Drucker]因安全原因下架，您的客户 WaveBone Persona 已进入重匹配审核" |


> **通知渠道**：V2 最小版本实现站内待办（审核工作台红点 + 待办计数）。飞书通知作为后期增强，当前只预留能力。

### 名言库管理流程（独立于客户审核）

名言库的入库审核是**独立流程**，解决"这条名言本身是否安全可用"，与上述客户绑定审核互不依赖。

```
  内容策划人录入 → draft
         │
    合规初审 → under_review
         │
    ┌────┴────┐
  通过      驳回 → 退回 draft
    │
  available（进入可选池，可被 HUI 匹配）
    │
  下架 ──┬── retired_normal（自然过期）
        │     → 已引用客户保持当前展示，下一周期自动重匹配
        │
        └── retired_safety（安全原因）
              → 触发所有引用客户立即重匹配
              → 重走三级审核
```

> **两个审核的关系**：名言入库审核（合规层，一次性）确保名言本身安全可用。客户绑定审核（业务层，每次绑定）确保该名言适合该客户。一条名言通过入库审核 ≠ 适合所有客户。

### 关键界面

#### 4.3 客户详情页 — Persona 卡片（V1 内部版）

```
┌─────────────────────────────────┐
│ Persona      [演示数据] [已匹配] │
│ 03/29 – 04/06                   │
│                                 │
│ Peter Drucker (1909–2005)       │
│ "Beware of little expenses;     │
│  a small leak will sink         │
│  a great ship."                 │
│                                 │
│        ╱ Retention ╲            │
│  Creativity      Exploration    │
│       ◆ ◇                       │
│  Risk Ctrl        Precision     │
│        ╲ Efficiency ╱           │
│                                 │
│ ● Current  ○ 7 Days Ago        │
│                                 │
│ Retention   72 +4               │
│ Exploration 68 +3               │
│ Precision   74 +4               │
│ Efficiency  78 +4               │
│ Risk Ctrl   72 +2               │
│ Creativity  85 +3               │
│                                 │
│          [VIEW ALL]             │
└─────────────────────────────────┘
```

### 后台管理页面

后台管理由三个页面组成，其中 Persona 相关页面在侧边栏合并为一个入口（`Persona 审核`），总览页从审核页内跳转访问。

#### 4.4 Persona 审核工作台（主页面）

这是日常使用频率最高的页面，用于处理 HUI 生成的 Persona 待审核事件。当前实现遵循以下规则：

- 仅保留“统一待审队列”，不再拆分“待人工选择”独立页签
- 页面不提供“当前角色切换”，角色由权限系统注入
- 顶部看板四项统计口径统一为“当前角色待审队列”
- 列表主动作统一为“进入审核/进入处理”，详情抽屉执行具体审核动作

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Around the Clock  >  Persona 审核                                      │
│                                                                         │
│  ┌─────────────────────────────────────────────┐   角色: 由权限系统注入    │
│  │ 客户选择  [全部客户 ▾]          搜索 [_______]│   ──────────────────   │
│  └─────────────────────────────────────────────┘                        │
│                                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                   │
│  │  ⊘ 已超时     │  │  ◔ 临近超时   │  │  ◉ 时效充足   │                   │
│  │     2        │  │     5        │  │     11       │                   │
│  └──────────────┘  └──────────────┘  └──────────────┘                   │
│                                                                         │
│  ┌───┬──────────┬──────────┬──────────┬──────────────┬────────┬───────┐ │
│  │ ☐ │ 客户      │ 雷达预览   │ 匹配名言   │ 匹配度/敏感度  │ 当前节点 │ 操作  │ │
│  ├───┼──────────┼──────────┼──────────┼──────────────┼────────┼───────┤ │
│  │ ☐ │ WaveBone │ ◇ 六轴缩略 │ "If it   │ 0.91  P2    │ ● 交付  │ 通过  │ │
│  │   │ 电商      │          │ doesn't  │ 常规         │   待审  │  ⋯   │ │
│  │   │          │          │ sell..." │              │        │       │ │
│  ├───┼──────────┼──────────┼──────────┼──────────────┼────────┼───────┤ │
│  │ ☐ │ GameOn   │ ◇ 六轴缩略 │ "Double  │ 0.89  P1    │ ● 行运  │ ──── │ │
│  │   │ 游戏      │          │ the num- │ 敏感         │   待审  │  ⋯   │ │
│  │   │          │          │ ber..." │              │        │       │ │
│  ├───┼──────────┼──────────┼──────────┼──────────────┼────────┼───────┤ │
│  │ ☐ │ ShopMax  │ ◇ 六轴缩略 │ Drucker    │ 0.43  P2    │ ● 交付  │ 进入  │ │
│  │   │ 电商      │          │ (低匹配)   │ 低匹配       │   待审  │ 处理  │ │
│  └───┴──────────┴──────────┴──────────┴──────────────┴────────┴───────┘ │
│                                                                         │
│  共 18 条  ｜  [☐ 3 项已选]  [批量通过]  [取消选择]                        │
└─────────────────────────────────────────────────────────────────────────┘
```

**列说明：**


| 列    | 内容                                               |
| ---- | ------------------------------------------------ |
| 雷达预览 | 缩略版 6 轴雷达（40×40px SVG），鼠标悬浮展开大图 + 分数             |
| 匹配名言 | 名言摘要（前 30 字），完整内容在详情 Drawer 中查看                  |
| 匹配度  | `quoteMatchScore`，< 0.5 标红并提示"低匹配待处理"            |
| 当前节点 | 复用 Around the Clock 的阶段色点（交付橙/行运青/销售紫/已发布灰/已拦截红） |


#### 4.5 审核详情 Drawer

点击表格行展开右侧 Drawer，包含完整审核信息和操作区。

```
┌────────────────────────────────────────────────┐
│  WaveBone · Persona 审核详情            [×]    │
│                                                │
│  [P2 常规]  [匹配度 0.91]  [● 交付待审]         │
│  评估周期：03/29 – 04/06                        │
│  ─────────────────────────────────────────     │
│                                                │
│  ◼ 匹配名言                                    │
│  ┌────────────────────────────────────────┐    │
│  │  David Ogilvy (1911–1999)              │    │
│  │  广告教父                               │    │
│  │                                        │    │
│  │  "If it doesn't sell, it isn't         │    │
│  │   creative."                           │    │
│  │                                        │    │
│  │  行业标签: ecommerce, gaming             │    │
│  │  亲和力: creativity 0.9 / efficiency    │    │
│  │          0.7 / precision 0.6           │    │
│  └────────────────────────────────────────┘    │
│                                                │
│  ◼ 能力雷达                                    │
│  ┌────────────────────────────────────────┐    │
│  │          ╱ Retention ╲                 │    │
│  │   Creativity      Exploration          │    │
│  │        ◆ ◇                             │    │
│  │   Risk Ctrl        Precision           │    │
│  │          ╲ Efficiency ╱                │    │
│  │                                        │    │
│  │  ● Current  ○ 7 Days Ago              │    │
│  │                                        │    │
│  │  Retention   72 (+4)                   │    │
│  │  Exploration 68 (+3)                   │    │
│  │  Precision   74 (+4)                   │    │
│  │  Efficiency  78 (+4)                   │    │
│  │  Risk Ctrl   72 (+2)                   │    │
│  │  Creativity  85 (+3)  ⚠ 变化较大        │    │
│  └────────────────────────────────────────┘    │
│                                                │
│  ◼ Around the Clock 关联事件                   │
│  ┌────────────────────────────────────────┐    │
│  │  09:15 Killed "Summer Vibes" at Hour 3 │    │
│  │  → Saved ¥8.2K · 已发布                 │    │
│  │                                        │    │
│  │  14:30 Found "Night Owl Parents"       │    │
│  │  → 3.4x ROAS · 已发布                  │    │
│  │                                        │    │
│  │  [查看全部 Around the Clock →]          │    │
│  └────────────────────────────────────────┘    │
│                                                │
│  ◼ 审核意见（可选）                              │
│  ┌────────────────────────────────────────┐    │
│  │  [输入审核意见...]                       │    │
│  └────────────────────────────────────────┘    │
│                                                │
│  ◼ 操作时间轴                                   │
│  ┌────────────────────────────────────────┐    │
│  │  ● HUI 生成  04/06 00:00  系统          │    │
│  │  ○ （等待交付审核）                       │    │
│  └────────────────────────────────────────┘    │
│                                                │
│  ──────────────────────────────────────────    │
│  [通过，提交行运]   [驳回]   [手动换名言 ▾]      │
└────────────────────────────────────────────────┘
```

**抽屉交互规范（已落地）：**

- “手动换名言/润色文案”跟随“匹配名言”模块头部
- “调整雷达”跟随“能力雷达”模块头部
- 底部固定操作区仅保留主决策：“通过，提交下一环节 / 驳回”
- “查看全部 Around the Clock”必须为真实跳转链接（携带 client 上下文）

**"手动换名言" 交互流程（Drawer 内操作）：**

点击 `[手动换名言 ▾]` 按钮后展开 Popover：

```
┌────────────────────────────────────────────┐
│  换名言                              [×]   │
│                                            │
│  当前: "If it doesn't sell, it isn't       │
│        creative." — David Ogilvy           │
│  匹配度 0.91                               │
│                                            │
│  搜索可用名言: [______________] 🔍          │
│                                            │
│  系统推荐（按匹配度排序）:                    │
│  ┌────────────────────────────────────┐    │
│  │  ○ "Beware of little expenses..." │    │
│  │    Peter Drucker · 匹配度 0.88     │    │
│  │                                    │    │
│  │  ○ "Double the number of          │    │
│  │    experiments..." — Jeff Bezos    │    │
│  │    匹配度 0.83                      │    │
│  │                                    │    │
│  │  ○ "知彼知己，百战不殆。"           │    │
│  │    孙子 · 匹配度 0.72              │    │
│  └────────────────────────────────────┘    │
│                                            │
│  换名言原因 *                               │
│  ┌──────────────────────────────────┐      │
│  │  [客户偏好/行业不符/调性不匹配...]  │      │
│  └──────────────────────────────────┘      │
│                                            │
│  ─────────────────────────────────────     │
│  [取消]                    [确认更换]       │
└────────────────────────────────────────────┘
```

**手动换名言规则：**


| 规则    | 说明                                                |
| ----- | ------------------------------------------------- |
| 可选范围  | 仅 `status = available` 的名言                        |
| 原因必填  | 更换必须填写原因（写入审计日志）                                  |
| 状态保持  | 更换后 `lockStatus` 不变（仍在当前审核节点），不重走前序审核             |
| 匹配度更新 | `quoteMatchScore` 按新名言重新计算并显示                     |
| 审计记录  | 时间轴新增一条"[张三] 手动更换名言：Ogilvy → Drucker，原因：客户偏好效率导向" |


**Drawer 分区说明：**


| 区域    | 内容                        | 审核辅助作用                   |
| ----- | ------------------------- | ------------------------ |
| 匹配名言  | 完整名言 + 作者信息 + 亲和力维度       | 判断名言本身是否合适               |
| 能力雷达  | 完整 6 轴双层雷达 + delta 标注     | 判断分数是否合理、变化是否需说明         |
| 关联事件  | 近期 Around the Clock 已发布事件 | 交叉验证雷达分数是否与 Agent 实际行为一致 |
| 审核意见  | 自由文本                      | 留痕，下游审核人可参考上游意见          |
| 操作时间轴 | 全流程审计记录                   | 了解前序操作历史                 |


#### 4.6 名言库管理页

用于内容策划人和合规审核员管理名言库。独立于客户审核流程。

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Around the Clock  >  名言库管理                                        │
│                                                                         │
│  状态筛选: [全部] [draft] [under_review] [available] [retired]            │
│  行业筛选: [全部 ▾]     语言: [全部 ▾]     搜索: [______________]         │
│                                                                         │
│  [+ 录入名言]                                                            │
│                                                                         │
│  ┌────┬─────────────────────────────────┬─────────┬────────┬──────────┐ │
│  │ #  │ 名言 / 作者                      │ 亲和力    │ 状态    │ 操作     │ │
│  ├────┼─────────────────────────────────┼─────────┼────────┼──────────┤ │
│  │ 1  │ "Beware of little expenses..."  │ Eff 0.9 │ ● 可用  │ 编辑     │ │
│  │    │ Peter Drucker · 管理学之父       │ Rsk 0.8 │ 5 客户  │ 下架 ▾  │ │
│  │    │ [ecommerce] [finance] [general] │ Prc 0.5 │ 引用中  │          │ │
│  ├────┼─────────────────────────────────┼─────────┼────────┼──────────┤ │
│  │ 2  │ "If it doesn't sell, it..."     │ Cre 0.9 │ ● 可用  │ 编辑     │ │
│  │    │ David Ogilvy · 广告教父          │ Eff 0.7 │ 2 客户  │ 下架 ▾  │ │
│  │    │ [ecommerce] [gaming]            │ Prc 0.6 │ 引用中  │          │ │
│  ├────┼─────────────────────────────────┼─────────┼────────┼──────────┤ │
│  │ 3  │ "知彼知己，百战不殆。"             │ Prc 0.8 │ ○ 待审  │ 审核     │ │
│  │    │ 孙子 · 兵法家                    │ Rsk 0.7 │        │          │ │
│  │    │ [gaming] [finance]              │ Ret 0.6 │        │          │ │
│  ├────┼─────────────────────────────────┼─────────┼────────┼──────────┤ │
│  │ 4  │ "Move fast and break things."   │ Exp 0.9 │ ◌ 已下  │ ────     │ │
│  │    │ Mark Zuckerberg                 │ Cre 0.7 │ 架(安全)│ 查看     │ │
│  │    │ ⚠ 安全下架 · 2025-04-01         │         │        │          │ │
│  └────┴─────────────────────────────────┴─────────┴────────┴──────────┘ │
│                                                                         │
│  统计: 10 条可用 · 2 条待审 · 1 条已下架  ｜  平均匹配分数: 0.87            │
└─────────────────────────────────────────────────────────────────────────┘
```

**名言录入 / 编辑 Dialog：**

```
┌──────────────────────────────────────────────┐
│  录入名言                               [×]  │
│                                              │
│  名言原文 *                                   │
│  ┌──────────────────────────────────────┐    │
│  │  [输入名言...]                        │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ┌──────────────────┐ ┌─────────────────┐    │
│  │ 语言              │ │ 归属人           │    │
│  │ [English ▾]       │ │ [输入姓名]       │    │
│  └──────────────────┘ └─────────────────┘    │
│                                              │
│  ┌──────────────────┐ ┌─────────────────┐    │
│  │ 生卒年             │ │ 头衔            │    │
│  │ [1909–2005]       │ │ [管理学之父]     │    │
│  └──────────────────┘ └─────────────────┘    │
│                                              │
│  行业标签                                     │
│  [ecommerce] [finance] [gaming]              │
│  [healthcare] [entertainment] [general]      │
│                                              │
│  维度亲和力标注（参考附录 A 规则）               │
│  ┌─────────────────────────────────────────┐ │
│  │  Retention    [====·····]  0.3          │ │
│  │  Exploration  [=·········]  0.1          │ │
│  │  Precision    [=====····]  0.5          │ │
│  │  Efficiency   [=========]  0.9  ← 主维度 │ │
│  │  Risk Ctrl    [========·]  0.8          │ │
│  │  Creativity   [=·········]  0.1          │ │
│  └─────────────────────────────────────────┘ │
│  ⓘ 至少 1 维 ≥ 0.7 · 至多 3 维 ≥ 0.5         │
│                                              │
│  ─────────────────────────────────────────   │
│  [取消]                [保存为草稿] [提交审核]  │
└──────────────────────────────────────────────┘
```

**下架确认 Dialog：**

```
┌──────────────────────────────────────────────┐
│  下架名言                               [×]  │
│                                              │
│  "Beware of little expenses; a small leak    │
│   will sink a great ship."                   │
│  — Peter Drucker                             │
│                                              │
│  下架类型 *                                   │
│  ○ 正常下架（自然过期）                        │
│    已引用客户保持展示至下一周期，审核界面标记提示    │
│                                              │
│  ● 安全下架（品牌/合规风险）                   │
│    ⚠ 将触发 5 个引用该名言客户重匹配            │
│    ⚠ 触发 HUI 重新匹配并进入审核流水线          │
│                                              │
│  下架原因 *                                   │
│  ┌──────────────────────────────────────┐    │
│  │  [输入原因...]                        │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  影响范围预览:                                 │
│  · WaveBone (auto_selected) → 进入重匹配         │
│  · QuickBuy (auto_selected) → 进入重匹配         │
│  · ShopMax (auto_selected) → 将清除绑定        │
│  · FinTech (auto_selected) → 进入重匹配          │
│  · PayEasy (auto_selected) → 进入重匹配          │
│                                              │
│  ─────────────────────────────────────────   │
│  [取消]                        [确认下架]      │
└──────────────────────────────────────────────┘
```

#### 4.7 客户 Persona 总览页

快速查看所有客户的 Persona 状态，发现异常。

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Persona 审核  >  查看 Persona 总览                                     │
│                                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │  已匹配       │  │  审核中       │  │  待处理       │  │  异常       │  │
│  │     5        │  │     2        │  │     1        │  │     0      │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  └────────────┘  │
│                                                                         │
│  ┌──────────┬────────┬──────────────┬──────────┬──────────┬───────────┐ │
│  │ 客户      │ 雷达缩略 │ 当前名言      │ 匹配度    │ 状态      │ 上次更新  │ │
│  ├──────────┼────────┼──────────────┼──────────┼──────────┼───────────┤ │
│  │ WaveBone │ ◇      │ Ogilvy       │ 0.91     │ ◔ 审核中  │ 04/02     │ │
│  │ QuickBuy │ ◇      │ Drucker      │ 0.88     │ ○ 算法推荐 │ 04/01     │ │
│  │ MedPlus  │ ◇      │ Deming       │ 0.93     │ ○ 算法推荐 │ 04/03     │ │
│  │ GameOn   │ ◇      │ Bezos        │ 0.89     │ ◔ 审核中  │ 04/06     │ │
│  │ FinTech  │ ◇      │ Buffett      │ 0.94     │ ○ 算法推荐 │ 04/02     │ │
│  │ ShopMax  │ ◇      │ Drucker      │ 0.85     │ ○ 算法推荐 │ 04/06     │ │
│  │ NovKing  │ ◇      │ Bernbach     │ 0.90     │ ○ 算法推荐 │ 04/04     │ │
│  │ PayEasy  │ ◇      │ Grove        │ 0.92     │ ○ 算法推荐 │ 04/03     │ │
│  └──────────┴────────┴──────────────┴──────────┴──────────┴───────────┘ │
│                                                                         │
│  匹配覆盖率: 5/8 (63%)  ｜  平均匹配分数: 0.90                            │
│                                                                         │
│  ◇ 名言库健康度: 10 可用 / 2 待审 / 1 已下架  ⚠ 可用库存不足（建议 ≥ 20 条）   │
└─────────────────────────────────────────────────────────────────────────┘
```

**客户数量扩展策略：**

此页面当前按线性列表渲染，适用于 ≤ 50 个客户。当客户规模增长时：


| 客户规模   | 策略                                  |
| ------ | ----------------------------------- |
| ≤ 50   | 线性列表，前端分页                           |
| 50–200 | 后端分页 + 搜索 + 按行业/状态筛选                |
| 200+   | 隐藏列表，改为"异常优先"视图（仅显示有异常的客户），全量数据按需加载 |


**异常标记规则：**


| 异常      | 触发条件                        | 显示            |
| ------- | --------------------------- | ------------- |
| 名言已下架   | 客户当前引用名言 `status = retired` | 行标红 + ⚠ 图标    |
| 匹配度过低   | `quoteMatchScore < 0.5`     | 匹配度列标红        |
| 长期未审核   | `auto_selected` 状态超过 7 天    | 状态列标橙         |
| 分数剧变    | 单维度周环比变化 > 15%              | 雷达缩略图标橙       |
| 名言库可用不足 | `available` 名言数量 < 20       | 顶部告警条"名言库存不足" |


### Edge Cases


| 场景                          | 处理                                                        |
| --------------------------- | --------------------------------------------------------- |
| 无可用名言（全部 retired/draft）     | 不展示名言区域，仅展示雷达 + 占位文案"暂无匹配名言"                              |
| 客户无 Snapshot 数据             | 雷达展示默认值（各维度 70），标注"暂无评估数据"，不显示名言                          |
| 名言因安全原因下架（`retired_safety`） | 触发所有引用该名言客户立即重匹配并重走审核，写入审计日志                              |
| 名言因正常原因下架（`retired_normal`） | 保持当前展示至下一周期，在审核界面标记"名言已下架"提示运营关注                          |
| 算法匹配分数极低（< 0.5）             | 标记低匹配风险，进入统一待审队列优先处理；必要时人工换名言或调雷达                         |
| 多条名言匹配分数相同                  | 优先选择行业标签匹配的，再按审核通过时间排序（优先已验证更久的）                          |
| V1 数据被误认为真实数据               | 所有 V1 界面强制展示"演示数据"标签，不可隐藏                                 |
| 有 `copyOverride` 时展示逻辑      | 对客展示 `copyOverride` 文案，审核 Drawer 同时展示原文和润色版，标注差异          |
| 名言原文被修改（修 typo 等）           | 不自动更新已有 `copyOverride`，发送通知给对应审核人确认是否需更新                  |
| 审核中途换名言                     | `lockStatus` 保持 `in_review` + 当前 `reviewStage` 不变，不重走前序审核 |
| 同一客户连续驳回 3 次                | 第 3 次驳回时系统标记"反复驳回"，升级通知给交付主管                              |


### 无障碍

- 雷达图 SVG 必须有 `aria-label="能力雷达"`
- Persona 状态标签使用语义化颜色 + 文字双重表达（颜色不是唯一信息载体）
- 名言区域 V2 使用 `blockquote` 语义标签
- "演示数据"标记使用 `role="status"` 确保屏幕阅读器可感知

---

## 实现清单

### V1 交付清单（内部验证）

以下为 v1.3 已落地的基础工程产出（内部演示可运行）。


| #    | 任务                                 | 文件                                                | 依赖         | 状态    |
| ---- | ---------------------------------- | ------------------------------------------------- | ---------- | ----- |
| V1-1 | `PersonaRadarDimensions` 6 维类型定义   | `lib/around-the-clock.ts`                         | 无          | ✅ 已完成 |
| V1-2 | `PersonaQuote` 类型 + 10 条种子数据       | `lib/around-the-clock.ts`                         | 无          | ✅ 已完成 |
| V1-3 | `PersonaSnapshot` 类型 + 种子快照数据      | `lib/around-the-clock.ts`                         | V1-1, V1-2 | ✅ 已完成 |
| V1-4 | `matchQuoteForRadar` 余弦相似度匹配       | `lib/around-the-clock.ts`                         | V1-2       | ✅ 已完成 |
| V1-5 | `canAlgorithmReplaceQuote` 自动替换规则  | `lib/around-the-clock.ts`                         | V1-3       | ✅ 已完成 |
| V1-6 | `CapabilityRadar` 6 轴 SVG 组件（双层对比） | `components/around-the-clock/CapabilityRadar.tsx` | V1-1       | ✅ 已完成 |
| V1-7 | 客户详情页 Persona 卡片集成                 | `app/client/[id]/page.tsx`                        | V1-3, V1-6 | ✅ 已完成 |


### 当前实现状态（v1.3）与下一阶段（v2 生产化）

本节说明两层信息：

1. **v1.3 当前实现状态**（已在代码中落地，内存 mock）
2. **v2 生产化事项**（数据库化、通知通道、并发一致性等）

**基础设施层（v1.3 当前状态）：**


| #     | 任务                                   | 文件                        | 依赖   | 状态          |
| ----- | ------------------------------------ | ------------------------- | ---- | ----------- |
| V2-A1 | `PersonaAuditEntry` 类型 + 审计写入/查询接口   | `lib/around-the-clock.ts` | 无    | ✅ 已完成（内存实现） |
| V2-A2 | Snapshot CRUD 接口（含 `reviewStage` 流转） | `lib/around-the-clock.ts` | V1-3 | ✅ 已完成（内存实现） |
| V2-A3 | 名言库 CRUD 接口（含状态流转）                   | `lib/around-the-clock.ts` | V1-2 | 🟡 部分完成     |


**页面层（v1.3 当前状态）：**


| #     | 任务          | 文件                                                    | 依赖           | 状态         |
| ----- | ----------- | ----------------------------------------------------- | ------------ | ---------- |
| V2-P1 | 审核工作台（列表页）  | `app/persona-review/page.tsx`                         | V2-A1, V2-A2 | ✅ 已完成      |
| V2-P2 | 审核详情 Drawer | `components/around-the-clock/PersonaReviewDrawer.tsx` | V2-P1        | ✅ 已完成      |
| V2-P3 | 名言库管理页      | `app/quote-library/page.tsx`                          | V2-A3        | ✅ 已完成（演示版） |
| V2-P4 | Persona 总览页 | `app/persona-overview/page.tsx`                       | V2-A2        | ✅ 已完成      |


---

## 5. Technical Considerations

### 数据模型

#### PersonaRadarDimensions（6 维雷达分数）

```typescript
type PersonaRadarDimensions = {
  retention: number     // 留存：预算平滑、续投率
  exploration: number   // 探索：新素材/新受众尝试意愿
  precision: number     // 精准：定向精度、出价精确度
  efficiency: number    // 效率：CPA/ROAS 优化速度
  riskControl: number   // 风控：异常拦截、超投保护
  creativity: number    // 创意：素材创新、文案多样性
}
```

#### PersonaQuote（名言库实体）

```typescript
type QuoteLibraryStatus = 'draft' | 'under_review' | 'available' | 'retired'
type QuoteRetiredReason = 'normal' | 'safety'

type PersonaQuote = {
  id: string
  text: string
  author: string
  authorLifespan: string
  authorTitle: string
  locale: 'en' | 'zh'
  dimensionAffinity: PersonaRadarDimensions  // 0–1 归一化，标注规则见附录 A
  tags: string[]                             // 行业适用标签
  status: QuoteLibraryStatus
  retiredType?: QuoteRetiredReason           // retired 时必填
  retiredReason?: string                     // 下架原因描述
  reviewedBy?: string
  reviewedAt?: string
}
```

#### PersonaSnapshot（客户 Persona 快照）

```typescript
type PersonaLockStatus = 'auto_selected' | 'in_review' | 'locked' | 'unlocked' | 'pending_manual'
type ReviewStage = 'at_delivery' | 'at_ops' | 'at_sales'

type PersonaSnapshot = {
  clientId: string
  evaluationPeriod: { start: string; end: string }
  current: PersonaRadarDimensions
  previous: PersonaRadarDimensions
  selectedQuoteId: string
  quoteMatchScore: number
  lockStatus: PersonaLockStatus
  reviewStage?: ReviewStage                  // lockStatus='in_review' 时必填
  rejectedFrom?: ReviewStage                 // 被驳回时记录"从哪一级退回"
  rejectedReason?: string                    // 驳回原因
  copyOverride?: string                      // 行运/销售润色后的展示文案，覆盖原名言 text
  generatedAt: string
  isDemo: boolean                            // V1 = true，标记为演示数据
  version: number                            // 快照版本号，每次 HUI 重新生成 +1
}

type PersonaAuditEntry = {
  snapshotClientId: string
  snapshotVersion: number
  action:
    | 'created'
    | 'review_pass'
    | 'review_reject'
    | 'quote_change'
    | 'copy_override'
    | 'radar_adjust'
    | 'lock'
    | 'unlock'
    | 'force_unlock'
  actor: string                              // 操作人 ID，系统操作为 'system'
  stage?: ReviewStage
  detail?: string                            // 驳回原因、换名言原因、润色内容等
  timestamp: string
}
```

> 多名言扩展说明（已决策）：当前 `PersonaSnapshot` 仍以 `selectedQuoteId` 支撑主 Persona 展示；后续将以“决策事件维度”新增子表（如 `PersonaDecisionQuote[]`）挂载多条名言，不影响当前主流程。

### 匹配算法

1. 将客户 6 维雷达分数归一化为向量
2. 计算每条 `available` 名言亲和力向量与客户向量的**余弦相似度**
3. 行业标签匹配加 0.05 bonus
4. 匹配分数 ≥ 0.5 时取最高分；< 0.5 时不自动绑定

```typescript
function matchQuoteForRadar(
  scores: PersonaRadarDimensions,
  industryTag?: string
): { quote: PersonaQuote; matchScore: number }
```

### 自动替换规则 + 安全下架

```typescript
function canAlgorithmReplaceQuote(snapshot: PersonaSnapshot): boolean {
  return snapshot.lockStatus !== 'pending_manual'
}

// V2：安全下架时触发所有引用客户重匹配
function forceRematchByQuoteId(quoteId: string): string[] {
  // 返回受影响的 clientId 列表，写入审计日志
}
```

### 状态模型补充说明

`PersonaLockStatus` 枚举中**不包含 `rejected`**。这是有意设计：驳回是一个**瞬时动作**而非持久状态。驳回发生时：

1. `lockStatus` 回退到上一级（交付驳回 → `auto_selected`；行运/销售驳回 → 退回上级的 `in_review`）
2. `rejectedFrom` 记录"从哪一级被退回"（用于 UI 显示"退回任务"标记）
3. `rejectedReason` 记录驳回原因

判断"当前是否为退回任务"的逻辑：

```typescript
function isRejectedTask(snapshot: PersonaSnapshot): boolean {
  return snapshot.rejectedFrom != null && snapshot.rejectedReason != null
}
```

当该 Snapshot 被重新提交审核时，清空 `rejectedFrom` 和 `rejectedReason`。

### API 接口契约

以下为 V2 后台页面所需的全部数据接口。V2 阶段为内存 mock 实现，函数签名固定，后续迁移数据库只替换实现体。

#### Snapshot 操作接口

```typescript
function getSnapshotsForReview(filter: {
  reviewStage?: ReviewStage
  lockStatus?: PersonaLockStatus
  clientId?: string
}): PersonaSnapshot[]

// 已落地函数（当前实现）
function approvePersonaSnapshot(params: {
  clientId: string
  actor?: string
  comment?: string
}): { success: boolean; snapshot?: PersonaSnapshot }

function rejectPersonaSnapshot(params: {
  clientId: string
  reason: string
  actor?: string
}): { success: boolean; snapshot?: PersonaSnapshot }

function changePersonaQuoteForClient(params: {
  clientId: string
  quoteId: string
  reason: string
  actor?: string
}): { success: boolean; snapshot?: PersonaSnapshot }

function setPersonaCopyOverrideForClient(params: {
  clientId: string
  text: string
  actor?: string
}): { success: boolean; snapshot?: PersonaSnapshot }

function adjustPersonaRadarForClient(params: {
  clientId: string
  nextCurrent: PersonaRadarDimensions
  reason: string
  actor?: string
}): { success: boolean; snapshot?: PersonaSnapshot }
```

#### 名言库操作接口

```typescript
function getQuotes(filter: {
  status?: QuoteLibraryStatus
  tag?: string
  locale?: 'en' | 'zh'
  search?: string
}): PersonaQuote[]

function createQuote(
  data: Omit<PersonaQuote, 'id' | 'status' | 'reviewedBy' | 'reviewedAt'>,
  actor: string
): PersonaQuote

function reviewQuote(
  quoteId: string,
  action: 'approve' | 'reject',
  actor: string,
  reason?: string
): { success: boolean }

function retireQuote(
  quoteId: string,
  retiredType: QuoteRetiredReason,
  reason: string,
  actor: string
): { success: boolean; affectedClients: string[] }

function getQuoteLibraryHealth(): {
  totalAvailable: number
  totalDraft: number
  totalRetired: number
  availableByTag: Record<string, number>
  staleDraftCount: number
  utilizationRate: number
}
```

#### 审计日志接口

```typescript
function logAuditEntry(entry: Omit<PersonaAuditEntry, 'timestamp'>): void

function getAuditTrail(clientId: string): PersonaAuditEntry[]
```

### 文件结构


| 文件                                                    | 职责                         |
| ----------------------------------------------------- | -------------------------- |
| `lib/around-the-clock.ts`                             | 类型定义、名言种子数据、匹配算法、审核写入、审计查询 |
| `components/around-the-clock/CapabilityRadar.tsx`     | 6 轴雷达 SVG 组件（支持双层对比）       |
| `components/around-the-clock/PersonaReviewDrawer.tsx` | 审核详情 Drawer 组件             |
| `app/client/[id]/page.tsx`                            | Persona 卡片集成               |
| `app/persona-review/page.tsx`                         | 审核工作台主页面                   |
| `app/quote-library/page.tsx`                          | 名言库管理页                     |
| `app/persona-overview/page.tsx`                       | 客户 Persona 总览页             |


### 快照保留策略

每个客户会随 HUI 周期性重新生成 Snapshot。历史快照的保留策略：


| 阶段           | 保留策略                                   |
| ------------ | -------------------------------------- |
| V1/V2 (Mock) | 仅保留最近 2 个版本（current + previous），内存存储   |
| V3 (生产)      | 保留最近 12 个版本（约 3 个月），数据库存储 + TTL 自动清理   |
| 审计日志         | 永久保留（合规要求），独立存储在 `PersonaAuditEntry` 表 |


> **数据一致性约束**：当 `copyOverride` 存在时，对客展示应使用 `copyOverride` 而非名言原文。若原名言被编辑（修复 typo 等），`copyOverride` 不自动更新——需由行运/销售重新确认。

### copyOverride 覆写规则

`copyOverride` 用于行运/销售对名言展示文案的润色（如语气调整、措辞优化），遵循以下规则：


| 规则    | 说明                                                                                |
| ----- | --------------------------------------------------------------------------------- |
| 写入时机  | 行运"润色"或销售"微调"操作时写入                                                                |
| 覆盖关系  | **后写覆盖前写**。若行运写了润色版，销售微调后覆盖行运版本                                                   |
| 历史保留  | 每次写入产生一条 `PersonaAuditEntry`（`action = 'copy_override'`），`detail` 字段记录完整的新旧文案，可追溯 |
| 清除时机  | HUI 生成新 Snapshot 版本时，`copyOverride` 自动清空（新周期从原文重新审核）                              |
| 展示优先级 | 对客展示：`copyOverride > quote.text`。审核 Drawer：同时展示原文和润色版，用 diff 标注差异                 |


### 名言库健康度指标

系统应持续监控名言库状态，在总览页顶部展示告警条：


| 指标       | 阈值               | 告警级别       |
| -------- | ---------------- | ---------- |
| 可用名言总数   | < 20 条           | ⚠ 警告       |
| 单行业可用名言  | < 3 条            | ⚠ 警告       |
| 待审名言堆积   | > 10 条超过 7 天未审   | 🔴 严重      |
| 可用名言被引用率 | > 80% 的名言被活跃客户引用 | ⚠ 警告（库存紧张） |


### 性能

- 名言匹配为 O(N) 线性遍历，当前库规模 ~10 条，无性能瓶颈
- 当名言库 > 500 条时，应预计算向量并建立行业标签索引
- 雷达 SVG 为纯计算渲染，无运行时依赖
- Snapshot 按 clientId 查询，当前为内存 mock，生产需加索引
- 审计日志写入异步化，不阻塞审核操作

### 安全 & 隐私

- 名言库不含用户数据，无隐私风险
- 雷达分数为 AI 行为聚合指标，不泄露投放细节
- 关键状态流转（审核通过/驳回/重匹配）需记录操作人和时间用于审计
- `retired_safety` 操作需记录完整影响范围
- 所有审核操作通过 `PersonaAuditEntry` 记录完整审计链
- `copyOverride` 修改同样需要审计记录

---

## 6. Decisions & Remaining Questions

### 6.1 已确认决策（本轮拍板）

| #   | 决策项 | 结论 | 对实现的影响 |
| --- | --- | --- | --- |
| D1 | 雷达分数计算公式（Q1） | 采用可解释的加权评分公式（见下），后续基于真实数据拟合权重 | 先可落地，再持续校准 |
| D2 | 名言亲和力标注标准（Q2） | 维持现状，按附录 A 执行，后续再优化 | 不阻断当前开发 |
| D3 | 名言库长期维护方式（Q3） | 工程维护为主，配合 AI 汇总整理 | 暂不建设 CMS |
| D4 | 评估周期（Q4） | 按日滚动（约 1 天完成），不做强约束 | 避免固定 7 天硬编码 |
| D5 | 单客户名言数量（Q5） | 支持多条，但按“不同 AI 决策事件”分别挂载 | 当前可单条展示，预留扩展 |
| D6 | 客户可调能力（Q6） | 客户侧不可调整 | 仅内部角色可操作 |
| D7 | 通知渠道（Q7） | 预留飞书接入能力，优先级不高 | 先站内，后飞书 |
| D8 | 权限体系（Q8） | 复用现有 RBAC，不额外开发 | 降低改造成本 |
| D9 | 润色文本能力（Q9） | 暂不支持富文本，保持纯文本 | 降低复杂度与风险 |

### 6.2 雷达分数公式建议（可执行 V1 方案）

> 目标：先给出可解释、可迭代、可落地的评分策略；后续再用真实数据回归拟合权重。

```text
score_dim = clamp(0, 100,
  100 * (
    w1 * normalized(metric_1) +
    w2 * normalized(metric_2) +
    w3 * normalized(metric_3) +
    ...
  ) - penalty + bonus
)
```

推荐初始映射（示例）：

- 留存（retention）= 续投率 + D7/D30 保留贡献 + 预算稳定性
- 探索（exploration）= 新素材尝试率 + 新受众覆盖率 + 实验次数
- 精准（precision）= 目标转化命中率 + 出价偏差反向指标 + 无效流量占比反向指标
- 效率（efficiency）= CPA 改善率 + ROAS 改善率 + 迭代耗时反向指标
- 风控（riskControl）= 异常拦截成功率 + 超预算防护率 + 风险恢复时长反向指标
- 创意（creativity）= 素材多样性指数 + 高表现新素材占比 + 创意衰减恢复速度

落地建议：

1. 初始权重由策略团队给定（版本化管理）
2. 每周用真实数据回测 MAE / Rank 相关性
3. 每月调整一次权重，保留变更记录（审计）

### 6.3 仍保留的开放项（不阻断）

| # | 议题 | 当前状态 | 备注 |
| --- | --- | --- | --- |
| O1 | 名言标注一致性提升机制（双人标注 + 一致性评分） | 待后续优化 | 当前先按附录 A 执行 |
| O2 | 飞书通知接入的具体事件粒度与模板 | 待后续排期 | 已明确预留能力，不阻断上线 |


---

## 附录 A：名言亲和力标注规则（初版）

> 此规则为 V1 种子数据标注时使用的参考框架，不代表最终标准。V2 需与内容策划人和算法团队共同修订。

### 标注流程

1. 阅读名言，提取核心主题词（如"节约"、"实验"、"数据"、"风险"）
2. 按下表将主题词映射到维度，给出 0–1 的亲和力评分
3. 由第二人独立标注，取两人均值，差异 > 0.3 的维度需讨论对齐

### 主题词 → 维度映射参考


| 主题词          | 主映射维度                 | 次映射维度                 |
| ------------ | --------------------- | --------------------- |
| 节约、成本、ROI、利润 | efficiency (0.7–0.9)  | riskControl (0.3–0.5) |
| 实验、创新、尝试、突破  | exploration (0.7–0.9) | creativity (0.5–0.7)  |
| 数据、测量、证据、精确  | precision (0.7–0.9)   | riskControl (0.3–0.5) |
| 风险、安全、谨慎、防御  | riskControl (0.7–0.9) | retention (0.3–0.5)   |
| 坚持、长期、复利、耐心  | retention (0.7–0.9)   | precision (0.2–0.4)   |
| 创意、洞察、人性、表达  | creativity (0.7–0.9)  | exploration (0.3–0.5) |


### 标注约束

- 每条名言至少有 1 个维度 ≥ 0.7（必须有明确的"强关联"）
- 每条名言至多有 3 个维度 ≥ 0.5（避免"万金油"标注稀释匹配区分度）
- 非相关维度应标注 0.1–0.3（而非 0），保持向量空间的连续性

